import React from 'react';

import './index.css';

const Options = ({ children }) => {
  return (
    <div className="mobile-dtable-ui-selector-editor-options">
      {children}
    </div>
  );
};

export default Options;
