import React from 'react';

import './index.css';

const Options = ({ children }) => {
  return (
    <div className="dtable-ui-mobile-selector-options">
      {children}
    </div>
  );
};

export default Options;
